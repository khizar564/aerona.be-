<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0"/>
  <title>Hotel Booking Confirmed - Aerona</title>
  <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;600;700&display=swap" rel="stylesheet">
  <style>
    body {
      font-family: "Poppins", sans-serif;
      background: #f1f4f9;
      margin: 0;
      padding: 40px 10px;
      color: #2c3e50;
    }

    .container {
      max-width: 750px;
      margin: auto;
      background: #fff;
      border-radius: 20px;
      overflow: hidden;
      box-shadow: 0 8px 30px rgba(0,0,0,0.08);
      animation: fadeIn 0.6s ease;
    }

    @keyframes fadeIn {
      from {opacity: 0; transform: translateY(20px);}
      to {opacity: 1; transform: translateY(0);}
    }

    /* HEADER */
    .header {
      background: linear-gradient(135deg, #0077b6, #0096c7);
      text-align: center;
      padding: 30px 20px;
      color: #fff;
    }

    .logo {
      max-width: 180px;
      margin-bottom: 10px;
    }

    .header h1 {
      margin: 0;
      font-size: 26px;
      font-weight: 700;
      letter-spacing: 0.5px;
    }

    /* CONFIRMATION */
    .confirmation-banner {
      background: #00b4d8;
      color: #fff;
      padding: 18px;
      text-align: center;
      font-weight: 600;
      font-size: 18px;
      letter-spacing: 0.3px;
    }

    /* INTRO */
    .intro-message {
      padding: 25px 25px 10px;
      line-height: 1.6;
      font-size: 15px;
      color: #555;
    }

    /* DETAILS */
    .booking-details {
      background: #fafafa;
      margin: 20px;
      border-radius: 16px;
      overflow: hidden;
      box-shadow: inset 0 0 0 1px #eaeaea;
    }

    .hotel-header {
      /* background: linear-gradient(135deg, #0096c7, #0077b6); */
      color:black;
      padding: 20px;
      text-align: center;
    }

    .hotel-name {
      margin: 0;
      font-size: 22px;
      font-weight: 700;
    }

    .details-grid {
      display: grid;
      grid-template-columns: 1fr 1fr;
      gap: 15px;
      padding: 20px;
    }

    .detail-item {
      background: #fff;
      border-radius: 10px;
      padding: 15px;
      display: flex;
      align-items: center;
      gap: 12px;
      box-shadow: 0 2px 6px rgba(0,0,0,0.05);
      transition: transform 0.2s;
    }

    .detail-item:hover { transform: translateY(-3px); }

    .detail-icon {
      font-size: 20px;
      color: #0096c7;
    }

    .detail-text { line-height: 1.3; }
    .detail-label {
      font-size: 11px;
      text-transform: uppercase;
      color: #888;
      font-weight: 600;
    }
    .detail-value {
      font-size: 15px;
      font-weight: 600;
      color: #2c3e50;
    }

    /* PRICE */
    .price-summary {
      margin: 20px;
      padding: 20px;
      background: #fff;
      border-radius: 14px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }

    .price-summary h3 {
      margin: 0 0 15px;
      font-size: 18px;
      color: #333;
    }

    .price-row {
      display: flex;
      justify-content: space-between;
      padding: 8px 0;
      font-size: 15px;
      border-bottom: 1px solid #eee;
    }

    .price-row:last-child {
      border-bottom: none;
      font-weight: 700;
      font-size: 17px;
      color: #0077b6;
    }

    /* INFO */
    .important-info {
      margin: 20px;
      padding: 18px;
      border-radius: 12px;
      background: #e7f5ff;
      border: 1px solid #cce5ff;
      color: #055160;
      font-size: 14px;
    }

    .important-info h3 {
      margin: 0 0 8px;
      font-size: 16px;
      font-weight: 600;
    }

  


    /* FOOTER */
    .footer {
      background: linear-gradient(135deg, #00b4d8, #0077b6);
      text-align: center;
      padding: 18px;
      font-size: 13px;
      color: #555;
      border-top: 1px solid #e5e5e5;
    }

    @media(max-width:600px){
      .details-grid { grid-template-columns: 1fr; }
    }
  </style>
</head>
<body>
  <div class="container">
    <!-- Header -->
    <div class="header">
      <img src="https://aeronna.s3.eu-north-1.amazonaws.com/Logo.png" alt="Aerona Logo" class="logo">
      <h1>There is a new hotel booking Booking with the following details</h1>
    </div>

    <!-- Banner -->
    <div class="confirmation-banner">
      Confirmation# AER-<%= bookingId %>
    </div>

    <!-- Intro -->
    <div class="intro-message">
      <p>
        Dear admin,<br><br>
        the booking is of hotel,  <strong><%= hotelName %></strong> has been confirmed! 🎉<br>
        Stay: <%= checkInDate %> → <%= checkOutDate %>.<br><br>
         Please find the booking details below.
      </p>
    </div>

    <!-- Details -->
     <div class="booking-details">
      <div class="hotel-header">
        <h2 class="hotel-name"><%= guestName %></h2>
      </div>
    <div class="booking-details">
      <div class="hotel-header">
        <h2 class="hotel-name"><%= hotelName %></h2>
      </div>

      <div class="details-grid">
        <div class="detail-item">
          <div class="detail-icon">📅</div>
          <div class="detail-text">
            <div class="detail-label">Check-in</div>
            <div class="detail-value"><%= checkInDate %></div>
          </div>
        </div>

        <div class="detail-item">
          <div class="detail-icon">📅</div>
          <div class="detail-text">
            <div class="detail-label">Check-out</div>
            <div class="detail-value"><%= checkOutDate %></div>
          </div>
        </div>

        <div class="detail-item">
          <div class="detail-icon">🛏️</div>
          <div class="detail-text">
            <div class="detail-label">Room</div>
            <div class="detail-value"><%= roomType %></div>
          </div>
        </div>

        <div class="detail-item">
          <div class="detail-icon">🌙</div>
          <div class="detail-text">
            <div class="detail-label">Nights</div>
            <div class="detail-value"><%= numberOfNights %> Nights</div>
          </div>
        </div>

        <div class="detail-item">
          <div class="detail-icon">👤</div>
          <div class="detail-text">
            <div class="detail-label">Guest</div>
            <div class="detail-value"><%= guestName %></div>
          </div>
        </div>
      </div>
    </div>

    <!-- Price -->
    <div class="price-summary">
      <h3>Price Summary</h3>
      <div class="price-row">
        <span>Total Amount</span>
        <span>$<%= totalAmount %></span>
      </div>
    </div>

   

    
    <!-- Footer -->
    <div class="footer">
      &copy; <%= year %> Aerona Travel · Need help?  
      <a href="mailto:support@aerona.com">support@aerona.com</a> · +1-800-AERONAA
    </div>
  </div>
</body>
</html>